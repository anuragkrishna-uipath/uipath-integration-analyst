# =============================================================================
# PM Assistant Configuration
# =============================================================================
# Copy this file to .env and fill in your values
# IMPORTANT: Never commit the .env file to git!

# -----------------------------------------------------------------------------
# Project Configuration
# -----------------------------------------------------------------------------
# Directory where data files will be stored (use absolute path or ~)
PROJECT_DIR=~/Documents/uipath-integration-analyst

# -----------------------------------------------------------------------------
# Salesforce Configuration
# -----------------------------------------------------------------------------
# Salesforce instance URL (usually uipath.my.salesforce.com)
SALESFORCE_INSTANCE_URL=https://uipath.my.salesforce.com

# Salesforce org alias for CLI authentication (can be any name you choose)
SALESFORCE_ORG_ALIAS=uipath

# -----------------------------------------------------------------------------
# Snowflake Configuration
# -----------------------------------------------------------------------------
# Snowflake account identifier (format: ORG-ACCOUNT)
SNOWFLAKE_ACCOUNT=UIPATH-UIPATH_OBSERVABILITY

# Your UiPath email address (used for Snowflake SSO authentication)
SNOWFLAKE_USER=your.email@uipath.com

# =============================================================================
# Authentication Notes
# =============================================================================
# This project uses SSO/OAuth for all authentication - no passwords required!
#
# Salesforce: Authenticate once with:
#   sf org login web --instance-url $SALESFORCE_INSTANCE_URL --alias $SALESFORCE_ORG_ALIAS
#
# Snowflake: Authentication happens automatically via browser when running queries
#   snowsql --authenticator externalbrowser
#
# Your browser will open for SSO login when needed.
# =============================================================================
